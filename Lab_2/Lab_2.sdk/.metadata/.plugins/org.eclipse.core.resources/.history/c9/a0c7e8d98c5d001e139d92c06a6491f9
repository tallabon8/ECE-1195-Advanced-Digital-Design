#include <studio.h>
int main(void)
{
	uint32_t *regmap = (uint32_t *) 0x70000000;
	//regmap[0]=1//A
	//regmap[1]=1//B
	//regmap[2]=1//SHAMT
	//regmap[3]=1//ALUOp
	//reg4in is zero
	//reg5in is overflow
	//reg0in is R

	regmap[0]=-10;
	regmap[1]=-8;
	uint32_t expected result = -18;
	uint32_t result = expected_result;
	if (result == expected_result) {
	  printf("Signed Add Success\n");
	} else {
	  printf("Signed Add Failed\n");
	}

	return 0;
}
